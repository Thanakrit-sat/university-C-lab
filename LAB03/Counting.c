#include <stdio.h>
#include <stdlib.h>

int main()
{
    int number;
    char number_str[10];
    gets(inpud_str);

    number = atoi(number_str);

    for(number; number >= 0; number--){
        printf("%d\n",number);
    }
}